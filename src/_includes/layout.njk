<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title or "Panos Tsomakos" }}</title>
  <link rel="stylesheet" href="/assets/css/styles.css">
  <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
</head>
<body>
  <header class="container">
    <h1>PANOS TSOMAKOS</h1>
    <nav>
      <a href="/private/">PRIVATE</a>
      <a href="#" id="commissioned-toggle">COMMISSIONED</a>
      <ul id="commissioned-menu">
        {% for project in collections.projects %}
          <li><a href="{{ project.url }}">{{ project.data.title }}</a></li>
        {% endfor %}
      </ul>
    </nav>
  </header>

  <main class="container">
    {{ content | safe }}
  </main>

  <script>
    const toggle = document.getElementById('commissioned-toggle');
    const menu = document.getElementById('commissioned-menu');
    toggle.addEventListener('click', (e) => {
      e.preventDefault();
      menu.classList.toggle('show');
    });

    // Handle Netlify Identity confirmation token
    if (window.netlifyIdentity) {
      window.netlifyIdentity.on("init", user => {
        const urlParams = new URLSearchParams(window.location.hash.replace("#", "?"));
        const token = urlParams.get("confirmation_token");
        if (token && !user) {
          window.netlifyIdentity.on("login", () => {
            window.location.href = "/admin/";
          });
          window.netlifyIdentity.open("login"); // Open login to process token
        } else if (user) {
          // If user is already logged in, redirect to admin
          if (window.location.pathname !== "/admin/") {
            window.location.href = "/admin/";
          }
        }
      });
    }
  </script>
</body>
</html>